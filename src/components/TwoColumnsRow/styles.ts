import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  columnsWrapper: {
    width: "100%",
    display: "flex",
    alignItems: "center",
    overflow: "hidden",
  },
  flexColumn: {
    flex: 1,
  },
});
